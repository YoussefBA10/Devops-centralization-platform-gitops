global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

# Alerting configuration (optional but recommended)
alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - alertmanager:9093

# Rule files
rule_files:
  - "alert_rules.yml"
  - "prediction_rules.yml"
  - "rules/*.yml"

# Scrape configurations
scrape_configs:

  # 1. Scrape the Monetique-Eye Backend
  - job_name: 'monetique-backend'
    metrics_path: '/actuator/prometheus'
    file_sd_configs:
      - files: ['/etc/prometheus/file_sd/agent_targets.yml']
    relabel_configs:
      - source_labels: [job]
        regex: 'monetique-backend'
        action: keep

  # 2. Central Node (central-node) - Self monitoring
  - job_name: 'central-node'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          environment: "central-node"
          role: "central-node"
          nodename: "central-node"

  # 3. Dynamic scraping of all Agent Nodes (Environment-aware)
  # Prometheus will automatically discover nodes based on labels added by Ansible
  - job_name: 'agent-nodes'
    metrics_path: '/metrics'
    honor_labels: true
    kubernetes_sd_configs: []   # Not using K8s - we use static + relabeling
    static_configs: []
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance

  # 4. Node Exporter (Host metrics) - scraped from all agents
  - job_name: 'node-exporter'
    metrics_path: '/metrics'
    file_sd_configs:
      - files:
          - '/etc/prometheus/file_sd/agent_targets.yml'
        refresh_interval: 15s
    relabel_configs:
      - source_labels: [job]
        regex: 'node-exporter'
        action: keep
      - source_labels: [instance]
        regex: '([^:]+):.*'
        target_label: instance
        replacement: '${1}'

  # 5. cAdvisor (Container metrics) - scraped from all agents
  - job_name: 'cadvisor'
    metrics_path: '/metrics'
    file_sd_configs:
      - files:
          - '/etc/prometheus/file_sd/agent_targets.yml'
        refresh_interval: 15s
    relabel_configs:
      - source_labels: [job]
        regex: 'cadvisor'
        action: keep
      - source_labels: [instance]
        regex: '([^:]+):.*'
        target_label: instance
        replacement: '${1}'

  # 6. Filebeat (Log agent metrics)
  - job_name: 'filebeat'
    metrics_path: '/metrics'
    file_sd_configs:
      - files:
          - '/etc/prometheus/file_sd/agent_targets.yml'
        refresh_interval: 15s
    relabel_configs:
      - source_labels: [job]
        regex: 'filebeat'
        action: keep

  # 7. Elasticsearch (DB Metrics)
  - job_name: 'elasticsearch'
    static_configs:
      - targets: ['elasticsearch-exporter:9114']
        labels:
          environment: "central-node"
          nodename: "central-node"

  # 8. Alertmanager (Self monitoring)
  - job_name: 'alertmanager'
    static_configs:
      - targets: ['alertmanager:9093']
        labels:
          environment: "central-node"
          nodename: "central-node"

  # 9. Grafana (Self monitoring)
  - job_name: 'grafana'
    static_configs:
      - targets: ['grafana:3000']
        labels:
          environment: "central-node"
          nodename: "central-node"

  # 11. Blackbox Exporter (Health probing for all nodes)
  - job_name: 'blackbox'
    metrics_path: '/probe'
    params:
      module: [http_2xx]
    file_sd_configs:
      - files:
          - '/etc/prometheus/file_sd/blackbox_targets.yml'
          - '/etc/prometheus/conf.d/network_monitor.yml'
        refresh_interval: 15s
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - source_labels: [probe_module]
        target_label: __param_module
      - target_label: __address__
        replacement: blackbox-exporter:9115